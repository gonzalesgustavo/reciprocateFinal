<% if is_bookmarked.present? %>
	<%= link_to "bookmarked", bookmark_path(is_bookmarked), method: :delete, remote: true %>
<% else %>
	<%= link_to post_bookmarks_path(post), method: :post, remote: true do %>
			<h2 class="btn btn-dark">bookmark</h2>
		<% end %>
<% end %>