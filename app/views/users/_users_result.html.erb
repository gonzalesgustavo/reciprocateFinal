<% if users.present? %>
	<% users.each do |user| %>
		<%= link_to user_path(user) do %>
			<div class="search-resutl-item"><%= user.name %></div>
		<% end %>
	<% end %>
<% else %>
	<div class="p-3">
		No results found.
	</div>
<% end %>